# Core ML Libraries
torch>=2.1.0
transformers>=4.51.0,<5.0.0
accelerate>=0.27.0

# PEFT and Quantization
peft>=0.10.0
bitsandbytes>=0.42.0

# Dataset and Hugging Face
datasets>=2.18.0
huggingface_hub>=0.21.0

# Training utilities
trl>=0.8.0
tensorboard>=2.16.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
einops>=0.7.0

# Jupyter (for notebook support)
jupyterlab>=4.0.0
ipywidgets>=8.0.0

# Optional: Flash Attention (for faster training)
# Uncomment if building with flash-attn support
# flash-attn>=2.5.0

# DeepSpeed for distributed training (DP + ZeRO optimizer/gradient sharding)
deepspeed>=0.13.0
